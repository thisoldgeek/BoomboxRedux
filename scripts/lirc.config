begin 
  remote = adafruit_mini_remote
  button = KEY_LEFT
  prog   = radio
  config = left
end

begin
 remote = adafruit_mini_remote
 button = KEY_RIGHT
 prog   = radio
 config = right
end

begin
 remote = adafruit_mini_remote
 button = KEY_UP
 prog   = radio
 config = prev
end

begin
 remote = adafruit_mini_remote
 button = KEY_DOWN
 prog   = radio
 config = next
end

begin
 remote = adafruit_mini_remote
 button = KEY_ENTER
 prog   = radio
 config = enter
end

begin
 remote = adafruit_mini_remote
 button = KEY_VOLUMEDOWN
 prog   = radio
 config = voldown
end

begin
 remote = adafruit_mini_remote
 button = KEY_VOLUMEUP
 prog   = radio
 config = volup
end

begin
 remote = adafruit_mini_remote
 button = KEY_BACK
 prog   = radio
 config = back
end

begin
 remote = adafruit_mini_remote
 button = KEY_PLAYPAUSE
 prog   = radio
 config = playpause
end

begin
 remote = adafruit_mini_remote
 button = KEY_SETUP
 prog   = radio
 config = setup
end

begin
 remote = adafruit_mini_remote
 button = KEY_STOP
 prog   = radio
 config = quit
end

begin
 remote = adafruit_mini_remote
 button = KEY_KP0
 prog   = radio
 config = bright
end

begin
 remote = adafruit_mini_remote
 button = KEY_KP1
 prog   = radio
 config = playlist1
end

begin
 remote = adafruit_mini_remote
 button = KEY_KP2
 prog   = radio
 config = playlist2
end

begin
 remote = adafruit_mini_remote
 button = KEY_KP3
 prog   = radio
 config = playlist3
end

begin
 remote = adafruit_mini_remote
 button = KEY_KP4
 prog   = radio
 config = playlist4
end



